Parte A — Backend (compilou com BUILD SUCCESS)
A1: AuthResponseDTO.java ganhou o campo role, preenchido no login em AuthService.java:73 — mudança aditiva, o mobile ignora.
A2: application.properties:46 agora inclui http://localhost:5173 no default do CORS (com comentário sobre a env de produção). Isso cobre REST e o handshake STOMP, pois o WebSocketConfig lê a mesma property — sem SockJS, como o plano alertava.
A3: três endpoints novos em TrainerController.java (/students/{id}/stats, /plan-evolution e o opcional /monthly-stats), todos passando pelo gate de posse novo TrainerLinkService.resolveOwnedStudentEmail, que responde 404 sem vínculo ATIVO (não vaza existência) e reusa o existsByTrainerIdAndStudentIdAndStatus já existente. Zero mudança em StatsService/WorkoutSessionService.
Parte B — Novo app kinetic-desktop/ (Vite + React + TS)
B1 (portas fiéis do mobile): tokenStorage.ts reimplementado com localStorage mantendo a interface assíncrona — com isso api.ts (interceptors + refresh coalescido) e chatService.ts (REST + STOMP) foram copiados trocando só EXPO_PUBLIC_API_URL → VITE_API_URL. jwt.ts, tipos e tokens de tema também portados; theme.css expõe os tokens como CSS variables e faz o reset de SPA desktop (página nunca rola; scroll só nos painéis internos).
B2: trainerService.ts (lado personal), AuthContext.tsx (barra login de papel ≠ PERSONAL com mensagem clara, registra o setSignOutHandler, restaura sessão via refresh token) e rotas /login, /students, /students/:id com ProtectedRoute.
B3: LoginPage, StudentsListPage com InviteStudentModal (guard de duplo-clique, 404/409 com mensagens amigáveis), e ChatPanel portando o state machine inteiro do TrainerChatScreen (paginação de 30, dedupe por id, STOMP-first com fallback REST, polling de 10s só com socket fora, filtro do peer na fila global) em bolhas DOM com column-reverse.
Dashboard: carreguei a skill dataviz antes dos gráficos. O ciano da marca #00E5FF falhou o check de banda de luminosidade para marcas em dark, então as séries usam o passo escurecido #00A5B8 (validado pelo script da skill: 4/4 PASS sobre #131313), mantendo #00E5FF nos acentos de UI. DashboardTab.tsx traz filtro de período, tiles de aderência/volume/peso, barras de volume por grupo muscular, linha de evolução de peso e o card de ciclo atual vs. anterior — cada card com estado vazio explícito no padrão Kinetic ("Nenhum treino registrado por … no período selecionado").
Verificação (backend real + Supabase dev)
tsc -b && vite build ✓ e 17/17 checagens de API PASS: registro PERSONAL/ALUNO, login retornando role, convite→aceite→carteira, os 3 endpoints de stats respondendo 200, 404 para aluno sem vínculo, 403 para aluno chamando endpoint de trainer, chat REST nos dois sentidos, preflight CORS da origem 5173 e handshake WebSocket 101 com Origin: http://localhost:5173 (403 para origem estranha). O graphify update . foi rodado.

Notas: ficaram três usuários de teste e2e.*@kinetic.test no banco de desenvolvimento (criados pela verificação prevista no plano) — me avise se quiser que eu os remova. Nada foi commitado, conforme o plano; a branch implementacaoWeb está com tudo pronto para você revisar. Para rodar: backend com o .env atual + cd kinetic-desktop && npm run dev (o .env do web já aponta para http://localhost:8080).